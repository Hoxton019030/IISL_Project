

server:
  port: 8080

spring:
  h2:
    console:
      enabled: true
      path: /h2-console #??URL????? localhost:8080/h2-console
  sql:
    init:
      mode: always #????????schema.sql??????sql??

  datasource:
    url: jdbc:h2:~/iisl;AUTO_SERVER=true  #??jdbc???url ?url? url:jdbc:h2:mem: ???????????(mem?????)
    driver-class-name: org.h2.Driver #??driver
    username: sa
    password: sa
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

